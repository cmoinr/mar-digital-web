---
const { title, description, href } = Astro.props;
---
<div class="rounded-lg border p-5 bg-white shadow-sm hover:shadow transition">
  <h3 class="text-lg font-semibold mb-2">{title}</h3>
  <p class="text-sm text-gray-600 mb-4">{description}</p>
  {href && <a href={href} class="text-brand hover:underline text-sm font-medium">Ver más →</a>}
</div>
