---
import BaseLayout from "../layouts/BaseLayout.astro";
import Footer from "../components/Footer.astro";

// Equipo - Perfiles clave
const teamProfiles = [
  {
    role: 'Project Manager',
    icon: '🎯',
    name: 'Gestión de Proyecto',
    description: 'Coordinación, timelines y comunicación clara en cada etapa.',
    color: 'from-blue-500 to-cyan-400'
  },
  {
    role: 'Diseñador',
    icon: '🎨',
    name: 'Diseño & Branding',
    description: 'Identidad visual, UX/UI y experiencias que convierten.',
    color: 'from-purple-500 to-pink-400'
  },
  {
    role: 'Developer',
    icon: '⚙️',
    name: 'Desarrollo',
    description: 'Código limpio, escalable y optimizado para performance.',
    color: 'from-cyan-500 to-blue-600'
  },
  {
    role: 'Performance',
    icon: '📊',
    name: 'Optimización',
    description: 'Análisis, métricas y mejora continua basada en datos.',
    color: 'from-green-500 to-emerald-400'
  }
];

// Método - 5 fases
const methodSteps = [
  { number: 1, name: 'Brief', description: 'Entendemos tu negocio, objetivos y audiencia clave.', icon: '📋' },
  { number: 2, name: 'Diseño', description: 'Creamos la identidad visual y la experiencia de usuario.', icon: '✏️' },
  { number: 3, name: 'Desarrollo', description: 'Construimos tu activo digital con tecnología de punta.', icon: '💻' },
  { number: 4, name: 'Lanzamiento', description: 'Desplegamos, configuramos tracking y monitoreamos.', icon: '🚀' },
  { number: 5, name: 'Optimización', description: 'Iteramos basados en datos reales y feedback continuo.', icon: '📈' }
];

// Valores
const coreValues = [
  {
    title: 'Claridad',
    description: 'Comunicación directa y transparente en cada etapa del proyecto.',
    icon: '💡'
  },
  {
    title: 'Cumplimiento',
    description: 'Entregamos en tiempo y forma lo que prometemos.',
    icon: '✓'
  },
  {
    title: 'ROI',
    description: 'Cada decisión está orientada a generar retorno medible.',
    icon: '📊'
  }
];
---
<BaseLayout title="Nosotros" description="Construimos activos digitales que crecen con tu negocio.">
  <!-- Hero Section -->
  <section class="relative pt-32 pb-24 lg:pt-40 lg:pb-32 overflow-hidden">
    <!-- Animated Background -->
    <div class="absolute inset-0 opacity-[0.03] pointer-events-none">
      <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,rgba(59,130,246,0.3),transparent_50%),radial-gradient(ellipse_at_bottom,rgba(6,182,212,0.2),transparent_50%)]"></div>
    </div>
    
    <!-- Floating particles -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none" aria-hidden="true">
      <div class="absolute top-20 left-[15%] w-2 h-2 rounded-full bg-blue-400/30 animate-pulse-glow"></div>
      <div class="absolute top-32 right-[20%] w-1.5 h-1.5 rounded-full bg-cyan-400/40 animate-pulse-glow" style="animation-delay: 1s;"></div>
      <div class="absolute bottom-40 left-[25%] w-1 h-1 rounded-full bg-blue-300/30 animate-pulse-glow" style="animation-delay: 2s;"></div>
    </div>

    <div class="relative max-w-5xl mx-auto px-6 text-center">
      <!-- Badge -->
      <div class="scroll-reveal inline-flex items-center gap-2 px-5 py-2.5 rounded-full bg-gradient-to-r from-blue-50 to-cyan-50 border border-blue-100/50 shadow-lg mb-8">
        <span class="w-2 h-2 rounded-full bg-gradient-to-r from-blue-500 to-cyan-400 animate-pulse"></span>
        <span class="text-xs font-semibold tracking-wide uppercase bg-gradient-to-r from-blue-700 to-cyan-600 bg-clip-text text-transparent">
          Nosotros
        </span>
      </div>

      <!-- Main Title -->
      <h1 class="scroll-reveal text-4xl sm:text-5xl lg:text-6xl xl:text-7xl font-extrabold leading-[1.1] mb-8 tracking-tight">
        <span class="inline-block bg-gradient-to-r from-gray-900 via-blue-800 to-gray-900 bg-clip-text text-transparent animate-gradient-shift bg-[length:200%_100%]">
          Quiénes Somos
        </span>
      </h1>

      <!-- Mission Statement -->
      <div class="scroll-reveal mb-12">
        <p class="text-2xl sm:text-3xl font-bold text-gray-800 leading-relaxed mb-4">
          Construimos <span class="bg-gradient-to-r from-blue-600 to-cyan-500 bg-clip-text text-transparent">activos digitales</span>
        </p>
        <p class="text-2xl sm:text-3xl font-bold text-gray-800 leading-relaxed">
          que <span class="bg-gradient-to-r from-cyan-600 to-blue-500 bg-clip-text text-transparent">crecen con tu negocio</span>
        </p>
      </div>

      <!-- Approach Description -->
      <p class="scroll-reveal text-lg sm:text-xl text-gray-600 leading-relaxed max-w-3xl mx-auto mb-12">
        Nuestro enfoque combina <strong class="text-blue-700">velocidad</strong>, <strong class="text-cyan-600">claridad de mensaje</strong>, 
        medición precisa y <strong class="text-blue-700">mejora continua</strong> para que tu presencia digital genere resultados reales.
      </p>

      <!-- Key Pillars -->
      <div class="scroll-reveal grid grid-cols-2 md:grid-cols-4 gap-4">
        {[
          { label: 'Velocidad', icon: '⚡' },
          { label: 'Claridad', icon: '💡' },
          { label: 'Medición', icon: '📊' },
          { label: 'Mejora continua', icon: '🔄' }
        ].map((pillar, index) => (
          <div 
            class="p-4 rounded-2xl bg-white/80 backdrop-blur-xl border border-blue-100/50 shadow-lg hover:shadow-xl hover:-translate-y-1 transition-all duration-300"
            style={`animation-delay: ${index * 100}ms;`}
          >
            <div class="text-3xl mb-2">{pillar.icon}</div>
            <div class="text-sm font-semibold text-gray-800">{pillar.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Valores Core -->
  <section class="relative py-24 lg:py-32 overflow-hidden">
    <!-- Background Effects -->
    <div class="absolute inset-0 bg-gradient-to-b from-white via-blue-50/20 to-white"></div>
    <div class="absolute inset-0 opacity-[0.03]">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_30%_40%,rgba(59,130,246,0.4),transparent_60%)]"></div>
    </div>

    <div class="relative max-w-7xl mx-auto px-6">
      <!-- Section Header -->
      <div class="text-center mb-16 scroll-reveal">
        <div class="inline-flex items-center gap-3 px-6 py-3 rounded-full bg-gradient-to-r from-blue-500 to-cyan-400 shadow-lg mb-6">
          <span class="text-3xl">⭐</span>
          <span class="text-white font-bold text-lg tracking-wide">Nuestros Valores</span>
        </div>
        <h2 class="text-3xl md:text-4xl lg:text-5xl font-extrabold text-gray-900 mb-4">
          Los pilares que nos <span class="bg-gradient-to-r from-blue-600 to-cyan-500 bg-clip-text text-transparent">definen</span>
        </h2>
        <p class="text-xl text-gray-700 max-w-3xl mx-auto leading-relaxed">
          Tres principios fundamentales que guían cada proyecto y decisión.
        </p>
      </div>

      <!-- Values Grid -->
      <div class="grid md:grid-cols-3 gap-8">
        {coreValues.map((value, index) => (
          <div 
            class="scroll-reveal group relative p-10 rounded-3xl bg-white/90 backdrop-blur-xl border-2 border-blue-100 shadow-xl hover:shadow-2xl hover:border-blue-300 transition-all duration-500 hover:-translate-y-2 overflow-hidden"
            style={`animation-delay: ${index * 150}ms;`}
          >
            <!-- Gradient Background -->
            <div class="absolute inset-0 bg-gradient-to-br from-blue-500/5 to-cyan-500/5 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
            
            <!-- Icon -->
            <div class="relative text-6xl mb-6 transform group-hover:scale-110 transition-transform duration-300">
              {value.icon}
            </div>

            <!-- Title -->
            <h3 class="relative text-2xl font-bold text-gray-900 mb-4">
              {value.title}
            </h3>

            <!-- Description -->
            <p class="relative text-gray-600 leading-relaxed">
              {value.description}
            </p>

            <!-- Decorative corner -->
            <div class="absolute -bottom-4 -right-4 w-24 h-24 bg-gradient-to-br from-blue-400/10 to-transparent rounded-full blur-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Método de Trabajo -->
  <section class="relative py-24 lg:py-32 overflow-hidden">
    <!-- Background Effects -->
    <div class="absolute inset-0 bg-gradient-to-b from-white via-cyan-50/20 to-white"></div>
    <div class="absolute inset-0 opacity-[0.03]">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_70%_50%,rgba(6,182,212,0.4),transparent_60%)]"></div>
    </div>

    <div class="relative max-w-7xl mx-auto px-6">
      <!-- Section Header -->
      <div class="text-center mb-16 scroll-reveal">
        <div class="inline-flex items-center gap-3 px-6 py-3 rounded-full bg-gradient-to-r from-cyan-500 to-blue-600 shadow-lg mb-6">
          <span class="text-3xl">🔄</span>
          <span class="text-white font-bold text-lg tracking-wide">Nuestro Método</span>
        </div>
        <h2 class="text-3xl md:text-4xl lg:text-5xl font-extrabold text-gray-900 mb-4">
          De la <span class="bg-gradient-to-r from-cyan-600 to-blue-500 bg-clip-text text-transparent">idea al impacto</span>
        </h2>
        <p class="text-xl text-gray-700 max-w-3xl mx-auto leading-relaxed">
          Un proceso probado en 5 fases que transforma tu visión en resultados medibles.
        </p>
      </div>

      <!-- Method Timeline -->
      <div class="relative max-w-5xl mx-auto">
        {methodSteps.map((step, index) => (
          <div 
            class="scroll-reveal relative mb-12 last:mb-0"
            style={`animation-delay: ${index * 100}ms;`}
          >
            <div class="flex items-start gap-6 lg:gap-8">
              <!-- Number Badge -->
              <div class="shrink-0">
                <div class="relative w-16 h-16 lg:w-20 lg:h-20 rounded-2xl bg-gradient-to-br from-blue-500 to-cyan-400 flex items-center justify-center text-white text-2xl lg:text-3xl font-bold shadow-xl">
                  {step.number}
                  <div class="absolute inset-0 rounded-2xl bg-gradient-to-br from-white/20 to-transparent"></div>
                </div>
              </div>

              <!-- Content Card -->
              <div class="flex-1 group relative p-6 lg:p-8 rounded-3xl bg-white/90 backdrop-blur-xl border border-blue-100 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1">
                <!-- Icon -->
                <div class="text-4xl mb-3">{step.icon}</div>
                
                <!-- Phase Name -->
                <h3 class="text-2xl font-bold text-gray-900 mb-3">
                  {step.name}
                </h3>
                
                <!-- Description -->
                <p class="text-gray-600 leading-relaxed text-lg">
                  {step.description}
                </p>

                <!-- Hover Gradient -->
                <div class="absolute inset-0 bg-gradient-to-br from-blue-500/5 to-cyan-500/5 rounded-3xl opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              </div>
            </div>

            <!-- Connecting Line -->
            {index < methodSteps.length - 1 && (
              <div class="absolute left-8 lg:left-10 top-20 w-0.5 h-12 bg-gradient-to-b from-blue-400 to-cyan-400 opacity-30"></div>
            )}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Equipo -->
  <section class="relative py-24 lg:py-32 overflow-hidden">
    <!-- Background Effects -->
    <div class="absolute inset-0 bg-gradient-to-b from-white via-purple-50/10 to-white"></div>
    <div class="absolute inset-0 opacity-[0.03]">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,rgba(139,92,246,0.3),transparent_60%)]"></div>
    </div>

    <div class="relative max-w-7xl mx-auto px-6">
      <!-- Section Header -->
      <div class="text-center mb-16 scroll-reveal">
        <div class="inline-flex items-center gap-3 px-6 py-3 rounded-full bg-gradient-to-r from-purple-500 to-pink-500 shadow-lg mb-6">
          <span class="text-3xl">👥</span>
          <span class="text-white font-bold text-lg tracking-wide">Nuestro Equipo</span>
        </div>
        <h2 class="text-3xl md:text-4xl lg:text-5xl font-extrabold text-gray-900 mb-4">
          Perfiles <span class="bg-gradient-to-r from-purple-600 to-pink-500 bg-clip-text text-transparent">especializados</span>
        </h2>
        <p class="text-xl text-gray-700 max-w-3xl mx-auto leading-relaxed">
          Expertos en cada área trabajando en sincronía para el éxito de tu proyecto.
        </p>
      </div>

      <!-- Team Grid -->
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
        {teamProfiles.map((profile, index) => (
          <div 
            class="scroll-reveal group relative p-8 rounded-3xl bg-white/90 backdrop-blur-xl border-2 border-gray-100 shadow-xl hover:shadow-2xl transition-all duration-500 hover:-translate-y-3 overflow-hidden text-center"
            style={`animation-delay: ${index * 100}ms;`}
          >
            <!-- Gradient Background -->
            <div class={`absolute inset-0 bg-gradient-to-br ${profile.color} opacity-0 group-hover:opacity-10 transition-opacity duration-500`}></div>
            
            <!-- Icon Badge -->
            <div class={`relative inline-flex items-center justify-center w-20 h-20 rounded-2xl bg-gradient-to-br ${profile.color} text-4xl shadow-xl mb-6 group-hover:scale-110 group-hover:rotate-6 transition-all duration-300`}>
              {profile.icon}
            </div>

            <!-- Role -->
            <div class="relative mb-2">
              <span class="text-xs font-bold uppercase tracking-wider text-gray-500">
                {profile.role}
              </span>
            </div>

            <!-- Name -->
            <h3 class="relative text-xl font-bold text-gray-900 mb-3">
              {profile.name}
            </h3>

            <!-- Description -->
            <p class="relative text-sm text-gray-600 leading-relaxed">
              {profile.description}
            </p>

            <!-- Decorative corner -->
            <div class={`absolute -bottom-4 -right-4 w-24 h-24 bg-gradient-to-br ${profile.color} rounded-full blur-3xl opacity-0 group-hover:opacity-20 transition-opacity duration-500`}></div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Final Section -->
  <section class="relative py-32 overflow-hidden">
    <!-- Animated Background -->
    <div class="absolute inset-0 bg-gradient-to-br from-slate-950 via-blue-950 to-slate-950"></div>
    
    <!-- Floating Blobs -->
    <div class="absolute inset-0 overflow-hidden opacity-40">
      <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-gradient-to-br from-blue-500/30 to-cyan-500/30 rounded-full blur-3xl animate-blob-float"></div>
      <div class="absolute bottom-1/3 right-1/4 w-80 h-80 bg-gradient-to-br from-cyan-400/20 to-blue-600/20 rounded-full blur-3xl animate-blob-float-delayed"></div>
    </div>

    <!-- Grid Pattern -->
    <div class="absolute inset-0 opacity-10 bg-[linear-gradient(to_right,rgba(255,255,255,0.1)_1px,transparent_1px),linear-gradient(to_bottom,rgba(255,255,255,0.1)_1px,transparent_1px)] bg-[size:60px_60px]"></div>

    <div class="relative max-w-5xl mx-auto px-6 text-center">
      <!-- Badge -->
      <div class="scroll-reveal inline-flex items-center gap-2 px-5 py-2.5 rounded-full bg-white/10 backdrop-blur-xl border border-white/20 mb-8 shadow-2xl">
        <span class="w-2 h-2 rounded-full bg-gradient-to-r from-blue-400 to-cyan-300 animate-pulse"></span>
        <span class="text-xs font-semibold tracking-wide uppercase text-white">
          ¿Listo para empezar?
        </span>
      </div>

      <!-- Main Title -->
      <h2 class="scroll-reveal text-4xl md:text-5xl lg:text-6xl font-extrabold leading-tight mb-8">
        <span class="bg-gradient-to-r from-white via-blue-100 to-cyan-100 bg-clip-text text-transparent">
          Conversemos de tu proyecto
        </span>
      </h2>

      <!-- Description -->
      <p class="scroll-reveal text-xl text-gray-300 max-w-3xl mx-auto leading-relaxed mb-12">
        Cuéntanos tu visión y objetivos. Te responderemos con una propuesta clara, 
        timeline realista y estrategia para hacer crecer tu negocio digital.
      </p>

      <!-- CTA Buttons -->
      <div class="scroll-reveal flex flex-col sm:flex-row gap-4 justify-center">
        <a 
          href="/contacto" 
          class="group relative inline-flex items-center justify-center gap-3 px-10 py-5 rounded-full bg-gradient-to-r from-blue-500 via-cyan-500 to-blue-600 text-white text-lg font-bold shadow-2xl hover:shadow-[0_0_50px_rgba(59,130,246,0.5)] transition-all duration-300 hover:scale-105 overflow-hidden"
        >
          <!-- Shine Effect -->
          <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-1000"></div>
          
          <span class="relative">💬 Iniciar conversación</span>
          
          <!-- Arrow -->
          <svg class="relative w-6 h-6 transform group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 7l5 5m0 0l-5 5m5-5H6" />
          </svg>
        </a>

        <a 
          href="/servicios" 
          class="inline-flex items-center justify-center gap-2 px-10 py-5 rounded-full bg-white/10 backdrop-blur-xl border border-white/20 text-white text-lg font-semibold hover:bg-white/20 transition-all duration-300 hover:scale-105"
        >
          <span>Ver nuestros servicios</span>
          <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </a>
      </div>

      <!-- Trust Indicators -->
      <div class="scroll-reveal mt-16 flex flex-wrap justify-center gap-8 text-gray-400 text-sm">
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-cyan-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
          <span>Respuesta en 24hs</span>
        </div>
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-blue-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
          <span>Propuesta personalizada</span>
        </div>
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-cyan-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
          <span>Sin compromiso</span>
        </div>
      </div>

      <!-- Decorative Elements -->
      <div class="absolute top-10 left-10 w-3 h-3 rounded-full bg-cyan-400/50 animate-pulse-glow"></div>
      <div class="absolute bottom-20 right-10 w-2 h-2 rounded-full bg-blue-400/50 animate-pulse-glow" style="animation-delay: 1s;"></div>
      <div class="absolute top-1/2 left-20 w-2 h-2 rounded-full bg-cyan-300/40 animate-pulse-glow" style="animation-delay: 2s;"></div>
    </div>
  </section>

  <Footer />
</BaseLayout>
