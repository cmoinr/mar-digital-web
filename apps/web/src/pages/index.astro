---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/Hero.jsx";
import SectionTitle from "../components/SectionTitle.astro";
import FeatureCard from "../components/FeatureCard.astro";
import Footer from "../components/Footer.astro";
import OptimizedImage from "../components/OptimizedImage.astro";

// Beneficios y servicios para la home
const beneficios = [
  { 
    text: 'Diseño profesional con enfoque estratégico',
    image: 'https://images.pexels.com/photos/3184292/pexels-photo-3184292.jpeg'
  },
  { 
    text: 'Procesos claros y acompañamiento constante',
    image: 'https://images.pexels.com/photos/3184296/pexels-photo-3184296.jpeg'
  },
  { 
    text: 'Paquetes adaptados a cada etapa de tu negocio',
    image: 'https://images.pexels.com/photos/3184325/pexels-photo-3184325.jpeg'
  },
  { 
    text: 'Resultados medibles y sostenibles',
    image: 'https://images.pexels.com/photos/3184297/pexels-photo-3184297.jpeg'
  }
];

const serviciosDestacados = [
  { icon: '💻', title: 'Diseño Web', description: 'Páginas y tiendas online que convierten visitantes en clientes.' },
  { icon: '🎨', title: 'Branding & Diseño', description: 'Identidades visuales que transmiten confianza.' },
  { icon: '📱', title: 'Marketing & Redes', description: 'Contenido y campañas que generan crecimiento.' },
  { icon: '📊', title: 'Consultoría Estratégica', description: 'Modelos de negocio, finanzas y transformación digital.' }
];
---
<BaseLayout title="Inicio" description="Impulsa tu marca con diseño y estrategia digital - Mar Digital">
  <!-- HERO PRINCIPAL -->
  <section class="relative overflow-hidden pt-32 pb-40 px-6 lg:pt-40 lg:pb-48">
    <!-- Background Image (replace 'YOUR_IMAGE_URL' with actual image URL) -->
    <div class="absolute inset-0 -z-10">
      <!-- Dark gradient overlay from left to right (40%) for text readability -->
      <div class="absolute inset-0 bg-black/50"></div>
      <OptimizedImage 
        src="https://images.pexels.com/photos/1933239/pexels-photo-1933239.jpeg" 
        alt="" 
        width={1920} 
        height={1080}
        priority={true}
        class="w-full h-full object-cover" 
      />
    </div>
    
    <!-- Decorative gradient overlay -->
    <div class="absolute inset-0 opacity-[0.03] pointer-events-none select-none" aria-hidden="true">
      <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top_left,rgba(59,130,246,0.3),transparent_50%),radial-gradient(ellipse_at_bottom_right,rgba(6,182,212,0.2),transparent_50%)]"></div>
    </div>
    
    <!-- Floating particles decoration -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none" aria-hidden="true">
      <div class="absolute top-20 left-[10%] w-2 h-2 rounded-full bg-blue-400/30 animate-pulse-glow"></div>
      <div class="absolute top-40 right-[15%] w-1.5 h-1.5 rounded-full bg-cyan-400/40 animate-pulse-glow" style="animation-delay: 1s;"></div>
      <div class="absolute bottom-32 left-[20%] w-1 h-1 rounded-full bg-blue-300/30 animate-pulse-glow" style="animation-delay: 2s;"></div>
    </div>

    <div class="relative max-w-7xl mx-auto">
      <!-- Hero Content -->
      <div class="relative max-w-3xl">
        <Hero
          headline="Impulsa tu marca con diseño y estrategia digital." 
          sub="En Mar Digital unimos creatividad y estrategia: desde páginas web y branding que hacen brillar tu negocio, hasta consultoría avanzada para que tu empresa crezca con bases sólidas." 
          primaryCtaText="Empieza tu proyecto hoy"
          primaryCtaHref="/contacto"
          secondaryCtaText="Explora nuestros paquetes"
          secondaryCtaHref="#planes"
          kicker=""
          client:load />
      </div>
    </div>
  </section>

  <!-- QUIÉNES SOMOS -->
  <section id="quienes-somos" class="relative py-28 px-6 bg-gradient-to-b from-slate-900 via-teal-950 to-slate-900">
    <div class="absolute inset-0 pointer-events-none opacity-[0.08] bg-[radial-gradient(ellipse_at_top,rgba(59,130,246,0.3),transparent_50%),radial-gradient(ellipse_at_bottom,rgba(6,182,212,0.2),transparent_50%)]"></div>
    <div class="relative max-w-4xl mx-auto scroll-reveal">
      <SectionTitle eyebrow="" title="¿Quiénes Somos?" sub="Somos un equipo que combina diseño creativo y consultoría estratégica. Nuestro propósito es ayudarte a construir una marca fuerte y un negocio preparado para crecer en digital." theme="dark" />
      <div class="mt-10 flex justify-center scroll-reveal">
        <a href="#divisiones" class="group relative inline-flex items-center gap-2 px-8 py-4 rounded-full bg-gradient-to-r from-blue-600 to-cyan-500 text-white font-semibold shadow-xl shadow-blue-500/30 hover:shadow-2xl hover:shadow-blue-500/40 transition-all duration-300 hover:scale-[1.02] overflow-hidden">
          <span class="relative z-10">Conoce más de nuestras divisiones</span>
          <svg class="relative z-10 w-5 h-5 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M19 9l-7 7-7-7"/>
          </svg>
          <span class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent translate-x-[-100%] group-hover:translate-x-[100%] transition-transform duration-700 skew-x-12"></span>
        </a>
      </div>
    </div>
  </section>

  <!-- DIVISIONES -->
  <section id="divisiones" class="py-28 px-6 relative">
    <!-- Background Image (replace 'YOUR_IMAGE_URL' with actual image URL) -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-gradient-to-br from-blue-900/15 via-cyan-900/55 to-blue-800/30"></div>
      <OptimizedImage 
        src="https://images.pexels.com/photos/14032494/pexels-photo-14032494.jpeg" 
        alt="" 
        width={1920} 
        height={1080}
        class="w-full h-full object-cover" 
      />
    </div>
    
    <!-- Decorative gradient overlay -->
    <div class="absolute inset-0 opacity-[0.03] pointer-events-none select-none" aria-hidden="true">
      <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(6,182,212,0.3),transparent_70%)]"></div>
    </div>
    
    <div class="relative max-w-6xl mx-auto">
      <div class="scroll-reveal">
        <SectionTitle eyebrow="Nuestras Divisiones" title="Dos focos, un mismo objetivo: crecimiento" theme="dark"/>
      </div>
      
      <div class="grid gap-8 md:grid-cols-2 mt-12">
        <!-- Creative Division Card -->
        <div class="scroll-reveal-left group relative p-10 rounded-3xl bg-white/10 backdrop-blur-xl border border-white/30 shadow-xl hover:shadow-2xl transition-all duration-500 overflow-hidden hover:-translate-y-2">
          <!-- Gradient glow on hover -->
          <div class="absolute -inset-1 bg-gradient-to-br from-blue-500/20 via-cyan-400/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 blur-xl"></div>
          
          <!-- Card content -->
          <div class="relative">
            <!-- Icon badge -->
            <div class="inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-gradient-to-br from-blue-500 to-cyan-400 shadow-lg shadow-blue-500/30 mb-6 group-hover:scale-110 transition-transform duration-300">
              <svg class="w-7 h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/>
              </svg>
            </div>
            
            <h3 class="text-2xl font-bold mb-4 text-white group-hover:text-blue-200 transition-colors">
              Mar Digital <span class="text-gradient-blue">Creative</span>
            </h3>
            
            <p class="text-base text-white/90 leading-relaxed mb-4">
              Diseño Web, Branding, Social Media y Marketing Digital.
            </p>
            
            <div class="flex items-start gap-2 p-4 rounded-xl bg-blue-500/20 border border-blue-300/30 backdrop-blur-sm">
              <svg class="w-5 h-5 text-blue-200 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
              </svg>
              <p class="text-sm text-white/80">
                Ideal para emprendedores y empresas que buscan identidad sólida y presencia real en línea.
              </p>
            </div>
          </div>
        </div>
        
        <!-- Business Division Card -->
        <div class="scroll-reveal-right group relative p-10 rounded-3xl bg-white/10 backdrop-blur-xl border border-white/30 shadow-xl hover:shadow-2xl transition-all duration-500 overflow-hidden hover:-translate-y-2">
          <!-- Gradient glow on hover -->
          <div class="absolute -inset-1 bg-gradient-to-br from-cyan-400/20 via-blue-500/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 blur-xl"></div>
          
          <!-- Card content -->
          <div class="relative">
            <!-- Icon badge -->
            <div class="inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-gradient-to-br from-cyan-500 to-blue-600 shadow-lg shadow-cyan-500/30 mb-6 group-hover:scale-110 transition-transform duration-300">
              <svg class="w-7 h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
              </svg>
            </div>
            
            <h3 class="text-2xl font-bold mb-4 text-white group-hover:text-cyan-200 transition-colors">
              Mar Digital <span class="text-gradient-cyan">Business</span>
            </h3>
            
            <p class="text-base text-white/90 leading-relaxed mb-4">
              Consultoría estratégica en modelos de negocio, transformación digital y análisis financiero.
            </p>
            
            <div class="flex items-start gap-2 p-4 rounded-xl bg-cyan-500/20 border border-cyan-300/30 backdrop-blur-sm">
              <svg class="w-5 h-5 text-cyan-200 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
              </svg>
              <p class="text-sm text-white/80">
                Pensado para empresas que necesitan claridad, estructura y rutas de crecimiento.
              </p>
            </div>
          </div>
        </div>
      </div>
      
      <div class="mt-16 text-center scroll-reveal">
        <a href="/about" class="group relative inline-flex items-center gap-2 px-8 py-4 rounded-full bg-white/10 backdrop-blur-xl text-white border border-white/30 font-semibold shadow-xl hover:shadow-2xl hover:bg-white/20 transition-all duration-300 hover:scale-[1.02] overflow-hidden">
          <span class="relative z-10">Conoce más de nuestras divisiones</span>
          <svg class="relative z-10 w-5 h-5 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
          </svg>
          <span class="absolute inset-0 bg-gradient-to-r from-white/5 via-white/10 to-white/5 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></span>
        </a>
      </div>
    </div>
  </section>

  <!-- BENEFICIOS -->
  <section id="beneficios" class="py-28 px-6 relative overflow-hidden bg-gradient-to-b from-slate-900 via-teal-950 to-slate-900">
    <div class="absolute inset-0 opacity-[0.08] bg-[radial-gradient(ellipse_at_center,rgba(59,130,246,0.3),transparent_70%)] pointer-events-none"></div>
    
    <div class="relative max-w-5xl mx-auto">
      <div class="scroll-reveal mb-16">
        <SectionTitle eyebrow="Beneficios" title="¿Por qué trabajar con nosotros?" theme="dark" />
      </div>
      
      <!-- Carousel Container -->
      <div class="relative">
        <!-- Cards Container -->
        <div class="relative h-[400px] sm:h-[450px] lg:h-[500px]">
          {beneficios.map((beneficio, index) => (
            <div 
              class="beneficio-card absolute inset-0 opacity-0 transition-opacity duration-1000"
              data-index={index}
              style={index === 0 ? 'opacity: 1;' : ''}
            >
              <!-- Card -->
              <div class="relative h-full rounded-3xl overflow-hidden shadow-2xl group">
                <!-- Background Image -->
                <OptimizedImage 
                  src={beneficio.image}
                  alt={beneficio.text}
                  width={1200}
                  height={800}
                  class="absolute inset-0 w-full h-full object-cover transition-transform duration-[10000ms] group-hover:scale-105"
                />
                
                <!-- Gradient Overlay -->
                <div class="absolute inset-0 bg-gradient-to-br from-blue-900/50 via-cyan-900/50 to-teal-800/70"></div>
                
                <!-- Content -->
                <div class="relative h-full flex flex-col items-center justify-center p-8 sm:p-12 text-center">
                  <h3 class="text-2xl sm:text-3xl lg:text-4xl font-bold text-white leading-tight max-w-3xl drop-shadow-lg">
                    {beneficio.text}
                  </h3>
                </div>
                
                <!-- Decorative corners -->
                <div class="absolute top-0 left-0 w-20 h-20 border-t-4 border-l-4 border-white/30 rounded-tl-3xl"></div>
                <div class="absolute bottom-0 right-0 w-20 h-20 border-b-4 border-r-4 border-white/30 rounded-br-3xl"></div>
              </div>
            </div>
          ))}
        </div>
        
        <!-- Progress Indicators -->
        <div class="flex justify-center gap-3 mt-8">
          {beneficios.map((_, index) => (
            <button 
              class="beneficio-indicator w-3 h-3 rounded-full transition-all duration-500"
              data-index={index}
              style={index === 0 ? 'background: linear-gradient(to right, #3b82f6, #06b6d4); width: 2rem;' : 'background: rgba(255,255,255,0.3);'}
              aria-label={`Ver beneficio ${index + 1}`}
            ></button>
          ))}
        </div>
      </div>
    </div>
  </section>
  
  <script>
    // Beneficios Carousel Logic
    let currentBeneficioIndex = 0;
    const beneficioCards = document.querySelectorAll('.beneficio-card');
    const beneficioIndicators = document.querySelectorAll('.beneficio-indicator');
    const totalBeneficios = beneficioCards.length;
    const autoPlayInterval = 7000; // 7 seconds
    let autoPlayTimer: number;

    function showBeneficio(index: number) {
      // Hide all cards
      beneficioCards.forEach((card) => {
        (card as HTMLElement).style.opacity = '0';
      });
      
      // Show current card
      (beneficioCards[index] as HTMLElement).style.opacity = '1';
      
      // Update indicators
      beneficioIndicators.forEach((indicator, i) => {
        if (i === index) {
          (indicator as HTMLElement).style.background = 'linear-gradient(to right, #3b82f6, #06b6d4)';
          (indicator as HTMLElement).style.width = '2rem';
        } else {
          (indicator as HTMLElement).style.background = 'rgba(255,255,255,0.3)';
          (indicator as HTMLElement).style.width = '0.75rem';
        }
      });
      
      currentBeneficioIndex = index;
    }

    function nextBeneficio() {
      const nextIndex = (currentBeneficioIndex + 1) % totalBeneficios;
      showBeneficio(nextIndex);
    }

    function startAutoPlay() {
      stopAutoPlay();
      autoPlayTimer = window.setInterval(nextBeneficio, autoPlayInterval);
    }

    function stopAutoPlay() {
      if (autoPlayTimer) {
        clearInterval(autoPlayTimer);
      }
    }

    // Indicator click handlers
    beneficioIndicators.forEach((indicator, index) => {
      indicator.addEventListener('click', () => {
        showBeneficio(index);
        startAutoPlay(); // Restart autoplay
      });
    });

    // Start autoplay
    startAutoPlay();

    // Pause on hover
    const beneficiosSection = document.querySelector('#beneficios');
    if (beneficiosSection) {
      beneficiosSection.addEventListener('mouseenter', stopAutoPlay);
      beneficiosSection.addEventListener('mouseleave', startAutoPlay);
    }
  </script>

  <!-- SERVICIOS DESTACADOS -->
  <section id="servicios" class="py-28 px-6 relative overflow-hidden">
    <!-- Background Image -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-black/50"></div>
      <OptimizedImage 
        src="https://images.pexels.com/photos/2127969/pexels-photo-2127969.jpeg" 
        alt="" 
        width={1920} 
        height={1080}
        class="w-full h-full object-cover" 
      />
    </div>
    
    <!-- Decorative overlay -->
    <div class="absolute inset-0 opacity-[0.05] pointer-events-none select-none" aria-hidden="true">
      <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(6,182,212,0.4),transparent_70%)]"></div>
    </div>
    
    <div class="relative max-w-7xl mx-auto">
      <div class="scroll-reveal mb-20 text-center">
        <SectionTitle eyebrow="Servicios destacados" title="Lo esencial para impulsar tu marca" theme="dark" />
      </div>
      
      <!-- Timeline Container -->
      <div class="relative scroll-reveal">
        <!-- Horizontal Line -->
        <div class="absolute top-8 left-0 right-0 h-1 bg-gradient-to-r from-blue-400 via-cyan-400 to-blue-400 rounded-full shadow-lg shadow-cyan-400/50"></div>
        
        <!-- Timeline Points -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-4">
          {serviciosDestacados.map((servicio, index) => (
            <div class="relative flex flex-col items-center scroll-reveal" style={`animation-delay: ${index * 150}ms`}>
              <!-- Point on timeline -->
              <div class="relative z-10 w-16 h-16 rounded-full bg-gradient-to-br from-blue-500 to-cyan-400 shadow-2xl shadow-blue-500/50 border-4 border-white mb-8 group-hover:scale-110 transition-all duration-500 flex items-center justify-center">
                <!-- Inner glow -->
                <div class="w-8 h-8 rounded-full bg-white animate-pulse"></div>
                
                <!-- Pulse ring animation -->
                <div class="absolute inset-0 rounded-full border-2 border-cyan-400 animate-ping opacity-75"></div>
              </div>
              
              <!-- Service Info Card -->
              <div class="group relative w-full max-w-xs">
                <!-- Glass card background -->
                <div class="relative p-6 rounded-2xl bg-white/10 backdrop-blur-xl border border-white/30 shadow-xl hover:shadow-2xl hover:bg-white/15 transition-all duration-500 hover:-translate-y-2">
                  <!-- Gradient glow on hover -->
                  <div class="absolute -inset-1 bg-gradient-to-br from-blue-400/30 via-cyan-400/30 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 blur-xl rounded-2xl"></div>
                  
                  <div class="relative text-center">
                    <h3 class="text-xl font-bold text-white mb-3 group-hover:text-cyan-200 transition-colors">
                      {servicio.title}
                    </h3>
                    
                    <p class="text-sm text-white/90 leading-relaxed">
                      {servicio.description}
                    </p>
                    
                    <!-- Decorative bottom line -->
                    <div class="mt-4 mx-auto h-1 w-16 bg-gradient-to-r from-cyan-400 to-blue-400 rounded-full group-hover:w-full transition-all duration-500"></div>
                  </div>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
      
      <div class="mt-20 text-center scroll-reveal">
        <a href="/servicios" class="group relative inline-flex items-center gap-2 px-8 py-4 rounded-full bg-white/10 backdrop-blur-xl text-white border border-white/30 font-semibold shadow-xl hover:shadow-2xl hover:bg-white/20 transition-all duration-300 hover:scale-[1.02] overflow-hidden">
          <span class="relative z-10">Ver todos los servicios</span>
          <svg class="relative z-10 w-5 h-5 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
          </svg>
          <span class="absolute inset-0 bg-gradient-to-r from-white/5 via-white/10 to-white/5 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></span>
        </a>
      </div>
    </div>
  </section>

  <!-- CTA FINAL -->
  <section class="relative py-32 lg:py-40 px-6 overflow-hidden">
    <!-- Background Image -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-black/50"></div>
      <OptimizedImage 
        src="https://images.pexels.com/photos/10050552/pexels-photo-10050552.jpeg?_gl=1*1rhn119*_ga*MTk2NzIzNDE3NC4xNzU4NjAwMjA1*_ga_8JE65Q40S6*czE3NjA3NDk0MzQkbzE1JGcxJHQxNzYwNzUyMzEyJGo2MCRsMCRoMA.." 
        alt="" 
        width={1920} 
        height={1080}
        class="w-full h-full object-cover" 
      />
    </div>
    
    <div class="relative max-w-4xl mx-auto text-center">
      <!-- Main headline -->
      <div class="scroll-reveal">
        <h2 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold leading-tight mb-10 tracking-tight">
          <span class="inline-block bg-gradient-to-r from-blue-400 via-cyan-400 to-blue-400 bg-clip-text text-transparent animate-gradient-shift bg-[length:200%_100%]">
            Haz crecer tu negocio hoy.
          </span>
        </h2>
      </div>
      
      <!-- Description -->
      <div class="scroll-reveal">
        <p class="mt-6 text-lg md:text-xl text-gray-300 leading-relaxed max-w-3xl mx-auto">
          No importa si apenas empiezas o ya lideras una empresa consolidada. En <strong class="text-blue-400">Mar Digital</strong> tenemos la combinación perfecta de <strong class="text-cyan-400">creatividad + estrategia</strong> para que tu marca brille en digital.
        </p>
      </div>
      
      <!-- CTA Button -->
      <div class="mt-14 flex flex-col sm:flex-row gap-4 justify-center items-center scroll-reveal">
        <a href="/contacto" class="group relative inline-flex items-center gap-3 px-10 py-5 rounded-full bg-gradient-to-r from-blue-600 via-cyan-500 to-blue-600 bg-[length:200%_100%] text-white font-bold text-lg shadow-2xl shadow-blue-500/40 hover:shadow-3xl hover:shadow-blue-500/50 transition-all duration-300 hover:bg-right hover:scale-[1.05] active:scale-[0.98] overflow-hidden">
          <span class="relative z-10">Quiero mi propuesta personalizada</span>
          <svg class="relative z-10 w-6 h-6 transition-transform duration-300 group-hover:translate-x-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
          </svg>
          <!-- Shine effect -->
          <span class="absolute inset-0 bg-gradient-to-r from-transparent via-white/30 to-transparent translate-x-[-100%] group-hover:translate-x-[100%] transition-transform duration-700 skew-x-12"></span>
          <!-- Pulse ring -->
          <span class="absolute inset-0 rounded-full border-2 border-white/50 animate-ping opacity-75"></span>
        </a>
      </div>
      
      <!-- Trust badges -->
      <div class="scroll-reveal mt-12 flex flex-wrap justify-center items-center gap-6 text-sm text-gray-400">
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
          </svg>
          <span>Sin compromiso</span>
        </div>
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
          </svg>
          <span>Respuesta en 24h</span>
        </div>
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
          </svg>
          <span>100% personalizado</span>
        </div>
      </div>
    </div>
  </section>
  <Footer />
</BaseLayout>
