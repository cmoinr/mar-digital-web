---
import BaseLayout from "../layouts/BaseLayout.astro";
import SectionTitle from "../components/SectionTitle.astro";
import Footer from "../components/Footer.astro";
import OptimizedImage from "../components/OptimizedImage.astro";

// Creative Services
const creativeServices = [
  {
    image: 'https://images.pexels.com/photos/326514/pexels-photo-326514.jpeg',
    title: 'Diseño y Desarrollo Web',
    description: 'Landing Pages, Websites Corporativos y E-commerce optimizados para conversión.',
    features: ['Responsive Design', 'SEO Optimizado', 'Performance']
  },
  {
    image: 'https://images.pexels.com/photos/1629236/pexels-photo-1629236.jpeg',
    title: 'Branding & Diseño Gráfico',
    description: 'Logos, identidades visuales y packs de contenido para una marca sólida y profesional.',
    features: ['Identidad Visual', 'Manual de Marca', 'Assets Digitales']
  },
  {
    image: 'https://images.pexels.com/photos/267350/pexels-photo-267350.jpeg',
    title: 'Social Media & Copywriting',
    description: 'Contenido gráfico + textos estratégicos para redes sociales que conectan y venden.',
    features: ['Content Strategy', 'Diseño de Posts', 'Copy Persuasivo']
  },
  {
    image: 'https://images.pexels.com/photos/265087/pexels-photo-265087.jpeg',
    title: 'Marketing Digital & Ads',
    description: 'Configuración de plataformas, gestión de Google Ads y Meta Ads para atraer más clientes.',
    features: ['Google Ads', 'Meta Ads', 'Analytics']
  }
];

// Business Services
const businessServices = [
  {
    image: 'https://images.pexels.com/photos/3184287/pexels-photo-3184287.jpeg',
    title: 'Consultoría en Modelos de Negocio',
    description: 'Diagnóstico y rediseño estratégico para maximizar el valor de tu empresa.'
  },
  {
    image: 'https://images.pexels.com/photos/3861969/pexels-photo-3861969.jpeg',
    title: 'Transformación Digital',
    description: 'Procesos, herramientas y KPIs para evolucionar tu empresa hacia el futuro digital.'
  },
  {
    image: 'https://images.pexels.com/photos/6801648/pexels-photo-6801648.jpeg',
    title: 'Valoración de Empresas',
    description: 'Métodos financieros avanzados para conocer el valor real de tu compañía.'
  },
  {
    image: 'https://images.pexels.com/photos/7567443/pexels-photo-7567443.jpeg',
    title: 'Análisis y Proyección Financiera',
    description: 'Modelos dinámicos, escenarios y planificación de capital inteligente.'
  },
  {
    image: 'https://images.pexels.com/photos/7567528/pexels-photo-7567528.jpeg',
    title: 'Estructuración de Capital',
    description: 'Estrategias para optimizar financiamiento y atraer inversionistas estratégicos.'
  }
];

// Paquetes Creative
const creativePacks = [
  { name: 'Starter', level: 'Emprendedores', color: 'from-blue-500 to-cyan-400' },
  { name: 'Growth', level: 'Marcas en expansión', color: 'from-cyan-500 to-blue-600' },
  { name: 'Premium', level: 'Presencia integral', color: 'from-blue-600 to-purple-500' }
];
---
<BaseLayout title="Servicios" description="En Mar Digital integramos creatividad y estrategia para que tu marca tenga presencia, identidad y resultados medibles en digital.">
  <!-- Hero Section -->
  <section class="relative pt-32 pb-24 lg:pt-40 lg:pb-32 overflow-hidden bg-gradient-to-b from-slate-900 via-teal-950 to-slate-900">
    <!-- Animated Background -->
    <div class="absolute inset-0 opacity-[0.08] pointer-events-none">
      <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,rgba(59,130,246,0.3),transparent_50%),radial-gradient(ellipse_at_bottom,rgba(6,182,212,0.2),transparent_50%)]"></div>
    </div>
    
    <!-- Floating particles -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none" aria-hidden="true">
      <div class="absolute top-20 left-[15%] w-2 h-2 rounded-full bg-blue-400/30 animate-pulse-glow"></div>
      <div class="absolute top-32 right-[20%] w-1.5 h-1.5 rounded-full bg-cyan-400/40 animate-pulse-glow" style="animation-delay: 1s;"></div>
      <div class="absolute bottom-40 left-[25%] w-1 h-1 rounded-full bg-blue-300/30 animate-pulse-glow" style="animation-delay: 2s;"></div>
    </div>

    <div class="relative max-w-5xl mx-auto px-6 text-center">
      <!-- Main Title -->
      <h1 class="scroll-reveal text-4xl sm:text-5xl lg:text-6xl xl:text-7xl font-extrabold leading-[1.1] mb-8 tracking-tight">
        <span class="inline-block bg-gradient-to-r from-blue-400 via-cyan-300 to-blue-400 bg-clip-text text-transparent animate-gradient-shift bg-[length:200%_100%]">
          Nuestros Servicios
        </span>
      </h1>

      <!-- Subtitle -->
      <p class="scroll-reveal text-lg sm:text-xl lg:text-2xl text-gray-300 leading-relaxed max-w-4xl mx-auto mb-12">
        En <strong class="text-blue-400">Mar Digital</strong> integramos <strong class="text-cyan-400">creatividad y estrategia</strong> para que tu marca tenga presencia, identidad y resultados medibles en digital.
      </p>

      <!-- Division Badges -->
      <div class="scroll-reveal flex flex-wrap justify-center gap-4">
        <a href="#creative-section" class="group px-6 py-3 rounded-2xl bg-white/10 backdrop-blur-xl border border-blue-300/30 shadow-lg hover:shadow-xl hover:bg-white/15 transition-all duration-300 hover:-translate-y-1 cursor-pointer">
          <div class="flex items-center gap-3">
            <span class="text-sm font-semibold text-blue-300">Mar Digital Creative</span>
          </div>
        </a>
        <a href="#business-section" class="group px-6 py-3 rounded-2xl bg-white/10 backdrop-blur-xl border border-cyan-300/30 shadow-lg hover:shadow-xl hover:bg-white/15 transition-all duration-300 hover:-translate-y-1 cursor-pointer">
          <div class="flex items-center gap-3">
            <span class="text-sm font-semibold text-cyan-300">Mar Digital Business</span>
          </div>
        </a>
      </div>
    </div>
  </section>

  <!-- Mar Digital Creative Section -->
  <section id="creative-section" class="relative py-24 lg:py-32 overflow-hidden">
    <!-- Background Image -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-gradient-to-br from-blue-900/85 via-cyan-900/80 to-blue-800/85"></div>
      <img src="https://images.pexels.com/photos/3184311/pexels-photo-3184311.jpeg" alt="" class="w-full h-full object-cover" />
    </div>
    
    <!-- Decorative overlay -->
    <div class="absolute inset-0 opacity-[0.05] pointer-events-none select-none" aria-hidden="true">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_30%_40%,rgba(59,130,246,0.4),transparent_60%)]"></div>
    </div>

    <div class="relative max-w-7xl mx-auto px-6">
      <!-- Section Header -->
      <div class="text-center mb-16 scroll-reveal">
        <div class="inline-flex items-center gap-3 px-6 py-3 rounded-full bg-gradient-to-r from-blue-500 to-cyan-400 shadow-lg mb-6">
          <span class="text-white font-bold text-lg tracking-wide">Mar Digital Creative</span>
        </div>
        <p class="text-xl text-white/90 max-w-3xl mx-auto leading-relaxed">
          Creamos soluciones digitales que <strong class="text-cyan-200">elevan la identidad</strong> de tu marca y generan <strong class="text-blue-200">confianza</strong> en tus clientes.
        </p>
      </div>

      <!-- Services Grid -->
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8 mb-16">
        {creativeServices.map((service, index) => (
          <div 
            class="scroll-reveal group relative rounded-3xl bg-white/10 backdrop-blur-xl border border-white/30 shadow-xl hover:shadow-2xl transition-all duration-500 hover:-translate-y-2 overflow-hidden flex flex-col"
            style={`animation-delay: ${index * 100}ms;`}
          >
            <!-- Hover Gradient -->
            <div class="absolute inset-0 bg-gradient-to-br from-blue-500/20 via-cyan-500/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
            
            <!-- Image Header -->
            <div class="relative h-48 overflow-hidden">
              <OptimizedImage 
                src={service.image} 
                alt={service.title}
                width={800}
                height={600}
                class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
              />
              <!-- Gradient overlay on image -->
              <div class="absolute inset-0 bg-gradient-to-b from-transparent via-transparent to-blue-900/60"></div>
            </div>

            <!-- Content -->
            <div class="relative p-6 flex flex-col flex-grow">
              <h3 class="text-xl font-bold text-white mb-3 group-hover:text-cyan-200 transition-colors">
                {service.title}
              </h3>
              <p class="text-white/90 leading-relaxed mb-6 flex-grow text-sm">
                {service.description}
              </p>

              <!-- Features -->
              {service.features && (
                <div class="flex flex-wrap gap-2">
                  {service.features.map(feature => (
                    <span class="px-3 py-1.5 rounded-full bg-blue-500/20 border border-blue-300/30 text-xs font-medium text-white/90">
                      {feature}
                    </span>
                  ))}
                </div>
              )}
            </div>

            <!-- Decorative corner -->
            <div class="absolute top-0 right-0 w-24 h-24 bg-gradient-to-br from-cyan-400/20 to-transparent rounded-bl-full opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
          </div>
        ))}
      </div>

      <!-- Paquetes Creative -->
      <div class="scroll-reveal">
        <div class="text-center mb-10">
          <h3 class="text-2xl font-bold text-white mb-3">Paquetes Creative</h3>
          <p class="text-white/80">Planes diseñados para cada etapa de tu crecimiento</p>
        </div>
        
        <div class="grid md:grid-cols-3 gap-6 max-w-5xl mx-auto">
          {creativePacks.map((pack, index) => (
            <div 
              class="group relative p-8 rounded-3xl bg-white/10 backdrop-blur-xl border border-white/30 shadow-xl hover:shadow-2xl transition-all duration-500 hover:-translate-y-2 overflow-hidden"
              style={`animation-delay: ${index * 150}ms;`}
            >
              <!-- Gradient Overlay -->
              <div class={`absolute inset-0 bg-gradient-to-br ${pack.color} opacity-0 group-hover:opacity-20 transition-opacity duration-500`}></div>
              
              <!-- Content -->
              <div class="relative text-center">
                <div class={`inline-block px-5 py-2 rounded-full bg-gradient-to-r ${pack.color} text-white font-bold text-lg mb-4 shadow-lg`}>
                  {pack.name}
                </div>
                <p class="text-white/90 font-medium">{pack.level}</p>
                
                <!-- Decorative line -->
                <div class="mt-6 h-1 w-20 mx-auto rounded-full bg-gradient-to-r from-transparent via-white/30 to-transparent"></div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Mar Digital Business Section -->
  <section id="business-section" class="relative py-24 lg:py-32 overflow-hidden">
    <!-- Background Image -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-gradient-to-br from-cyan-900/85 via-blue-900/80 to-cyan-800/85"></div>
      <img src="https://images.pexels.com/photos/3184338/pexels-photo-3184338.jpeg" alt="" class="w-full h-full object-cover" />
    </div>
    
    <!-- Decorative overlay -->
    <div class="absolute inset-0 opacity-[0.05] pointer-events-none select-none" aria-hidden="true">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_70%_50%,rgba(6,182,212,0.4),transparent_60%)]"></div>
    </div>

    <div class="relative max-w-7xl mx-auto px-6">
      <!-- Section Header -->
      <div class="text-center mb-16 scroll-reveal">
        <div class="inline-flex items-center gap-3 px-6 py-3 rounded-full bg-gradient-to-r from-cyan-500 to-blue-600 shadow-lg mb-6">
          <span class="text-white font-bold text-lg tracking-wide">Mar Digital Business</span>
        </div>
        <p class="text-xl text-white/90 max-w-3xl mx-auto leading-relaxed">
          Acompañamos a empresas y startups con <strong class="text-cyan-200">consultoría estratégica</strong> para crecer con bases sólidas y <strong class="text-blue-200">visión a futuro</strong>.
        </p>
      </div>

      <!-- Services Grid -->
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        {businessServices.map((service, index) => (
          <div 
            class="scroll-reveal group relative rounded-3xl bg-white/10 backdrop-blur-xl border border-white/30 shadow-xl hover:shadow-2xl transition-all duration-500 hover:-translate-y-2 overflow-hidden flex flex-col"
            style={`animation-delay: ${index * 100}ms;`}
          >
            <!-- Hover Gradient -->
            <div class="absolute inset-0 bg-gradient-to-br from-cyan-500/20 via-blue-500/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
            
            <!-- Image Header -->
            <div class="relative h-48 overflow-hidden">
              <OptimizedImage 
                src={service.image} 
                alt={service.title}
                width={800}
                height={600}
                class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
              />
              <!-- Gradient overlay on image -->
              <div class="absolute inset-0 bg-gradient-to-b from-transparent via-transparent to-cyan-900/60"></div>
            </div>

            <!-- Content -->
            <div class="relative p-6 flex flex-col flex-grow">
              <h3 class="text-xl font-bold text-white mb-3 group-hover:text-cyan-200 transition-colors leading-tight">
                {service.title}
              </h3>
              <p class="text-white/90 leading-relaxed text-sm flex-grow">
                {service.description}
              </p>
            </div>

            <!-- Decorative corner -->
            <div class="absolute top-0 right-0 w-24 h-24 bg-gradient-to-br from-cyan-400/20 to-transparent rounded-bl-full opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
          </div>
        ))}
      </div>

      <!-- Bottom Accent -->
      <div class="scroll-reveal mt-16 text-center">
        <div class="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-white/10 backdrop-blur-xl border border-white/30 shadow-lg">
          <span class="text-sm font-medium text-white/90">Estrategias basadas en datos y experiencia comprobada</span>
          <svg class="w-5 h-5 text-cyan-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
          </svg>
        </div>
      </div>
    </div>
  </section>

  <!-- Final CTA Section -->
  <section class="relative py-32 overflow-hidden">
    <!-- Animated Background -->
    <div class="absolute inset-0 bg-gradient-to-br from-slate-950 via-blue-950 to-slate-950"></div>
    
    <!-- Floating Blobs -->
    <div class="absolute inset-0 overflow-hidden opacity-40">
      <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-gradient-to-br from-blue-500/30 to-cyan-500/30 rounded-full blur-3xl animate-blob-float"></div>
      <div class="absolute bottom-1/3 right-1/4 w-80 h-80 bg-gradient-to-br from-cyan-400/20 to-blue-600/20 rounded-full blur-3xl animate-blob-float-delayed"></div>
    </div>

    <!-- Grid Pattern -->
    <div class="absolute inset-0 opacity-10 bg-[linear-gradient(to_right,rgba(255,255,255,0.1)_1px,transparent_1px),linear-gradient(to_bottom,rgba(255,255,255,0.1)_1px,transparent_1px)] bg-[size:60px_60px]"></div>

    <div class="relative max-w-5xl mx-auto px-6 text-center">
      <!-- Badge -->
      <div class="scroll-reveal inline-flex items-center gap-2 px-5 py-2.5 rounded-full bg-white/10 backdrop-blur-xl border border-white/20 mb-8 shadow-2xl">
        <span class="w-2 h-2 rounded-full bg-gradient-to-r from-blue-400 to-cyan-300 animate-pulse"></span>
        <span class="text-xs font-semibold tracking-wide uppercase text-white">
          ¿Listo para empezar?
        </span>
      </div>

      <!-- Main Title -->
      <h2 class="scroll-reveal text-4xl md:text-5xl lg:text-6xl font-extrabold leading-tight mb-8">
        <span class="bg-gradient-to-r from-white via-blue-100 to-cyan-100 bg-clip-text text-transparent">
          Ya sea que necesites creatividad para tu marca
        </span>
        <br />
        <span class="bg-gradient-to-r from-cyan-300 via-blue-300 to-white bg-clip-text text-transparent">
          o consultoría estratégica para tu empresa
        </span>
      </h2>

      <!-- Description -->
      <p class="scroll-reveal text-xl text-gray-300 max-w-3xl mx-auto leading-relaxed mb-12">
        En <strong class="text-white">Mar Digital</strong> tenemos el servicio perfecto para ti. 
        Conectemos y construyamos algo extraordinario juntos.
      </p>

      <!-- CTA Button -->
      <div class="scroll-reveal">
        <a 
          href="/contacto" 
          class="group relative inline-flex items-center gap-3 px-10 py-5 rounded-full bg-gradient-to-r from-blue-500 via-cyan-500 to-blue-600 text-white text-lg font-bold shadow-2xl hover:shadow-[0_0_50px_rgba(59,130,246,0.5)] transition-all duration-300 hover:scale-105 overflow-hidden"
        >
          <!-- Shine Effect -->
          <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-1000"></div>
          
          <!-- Content -->
          <span class="relative">👉 Solicita tu propuesta personalizada</span>
          
          <!-- Arrow -->
          <svg class="relative w-6 h-6 transform group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 7l5 5m0 0l-5 5m5-5H6" />
          </svg>
        </a>
      </div>

      <!-- Trust Indicators -->
      <div class="scroll-reveal mt-16 flex flex-wrap justify-center gap-8 text-gray-400 text-sm">
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-cyan-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
          <span>Sin compromiso</span>
        </div>
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-blue-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
          <span>Respuesta en 24hs</span>
        </div>
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-cyan-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
          <span>Propuesta personalizada</span>
        </div>
      </div>

      <!-- Decorative Elements -->
      <div class="absolute top-10 left-10 w-3 h-3 rounded-full bg-cyan-400/50 animate-pulse-glow"></div>
      <div class="absolute bottom-20 right-10 w-2 h-2 rounded-full bg-blue-400/50 animate-pulse-glow" style="animation-delay: 1s;"></div>
      <div class="absolute top-1/2 left-20 w-2 h-2 rounded-full bg-cyan-300/40 animate-pulse-glow" style="animation-delay: 2s;"></div>
    </div>
  </section>

  <Footer />
</BaseLayout>
