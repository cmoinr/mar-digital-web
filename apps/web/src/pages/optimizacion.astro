---
import BaseLayout from "../layouts/BaseLayout.astro";
import Footer from "../components/Footer.astro";

// Checklist de 20 puntos del diagnóstico
const diagnosticChecklist = [
  { category: 'Performance', items: ['Tiempo de carga (LCP)', 'First Input Delay', 'Cumulative Layout Shift', 'Optimización de imágenes'] },
  { category: 'SEO Técnico', items: ['Meta tags y estructura', 'Sitemap y robots.txt', 'Enlaces rotos', 'Velocidad móvil'] },
  { category: 'Conversión', items: ['Claridad de CTAs', 'Flujo de navegación', 'Formularios optimizados', 'Trust signals'] },
  { category: 'Tracking', items: ['Google Analytics 4', 'Facebook Pixel', 'Eventos clave', 'Conversiones configuradas'] },
  { category: 'UX/UI', items: ['Responsive design', 'Accesibilidad', 'Jerarquía visual', 'Consistencia de marca'] }
];

// Paquetes de optimización
const packages = [
  {
    name: 'Performance',
    icon: '⚡',
    price: 'Desde $490 USD',
    description: 'Velocidad, SEO técnico y optimización de conversión básica',
    color: 'from-blue-500 to-cyan-400',
    features: [
      'Análisis de Core Web Vitals',
      'Optimización de imágenes y assets',
      'Mejoras de velocidad de carga',
      'SEO técnico (meta tags, estructura)',
      'Auditoría básica de CRO',
      'Reporte de métricas y recomendaciones'
    ],
    badge: 'Más Popular',
    highlight: true
  },
  {
    name: 'Ads + Tracking',
    icon: '📊',
    price: 'Desde $590 USD',
    description: 'Estructura publicitaria, píxeles y medición profesional',
    color: 'from-cyan-500 to-blue-600',
    features: [
      'Configuración de Google Ads',
      'Instalación de Meta Pixel',
      'Google Analytics 4 completo',
      'Eventos de conversión personalizados',
      'Dashboard de métricas clave',
      'Documentación y capacitación'
    ],
    badge: 'Recomendado',
    highlight: false
  }
];
---
<BaseLayout title="Optimiza tu negocio" description="Diagnóstico express gratuito y paquetes de mejora para potenciar tu presencia digital.">
  <!-- Hero Section -->
  <section class="relative pt-32 pb-24 lg:pt-40 lg:pb-32 overflow-hidden">
    <!-- Animated Background -->
    <div class="absolute inset-0 opacity-[0.03] pointer-events-none">
      <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top_left,rgba(59,130,246,0.3),transparent_50%),radial-gradient(ellipse_at_bottom_right,rgba(6,182,212,0.2),transparent_50%)]"></div>
    </div>
    
    <!-- Floating particles -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none" aria-hidden="true">
      <div class="absolute top-24 left-[10%] w-2 h-2 rounded-full bg-blue-400/30 animate-pulse-glow"></div>
      <div class="absolute top-36 right-[15%] w-1.5 h-1.5 rounded-full bg-cyan-400/40 animate-pulse-glow" style="animation-delay: 1s;"></div>
      <div class="absolute bottom-32 left-[20%] w-1 h-1 rounded-full bg-blue-300/30 animate-pulse-glow" style="animation-delay: 2s;"></div>
    </div>

    <div class="relative max-w-5xl mx-auto px-6 text-center">
      <!-- Badge -->
      <div class="scroll-reveal inline-flex items-center gap-2 px-5 py-2.5 rounded-full bg-gradient-to-r from-blue-50 to-cyan-50 border border-blue-100/50 shadow-lg mb-8">
        <span class="w-2 h-2 rounded-full bg-gradient-to-r from-blue-500 to-cyan-400 animate-pulse"></span>
        <span class="text-xs font-semibold tracking-wide uppercase bg-gradient-to-r from-blue-700 to-cyan-600 bg-clip-text text-transparent">
          Diagnóstico y Optimización
        </span>
      </div>

      <!-- Main Title -->
      <h1 class="scroll-reveal text-4xl sm:text-5xl lg:text-6xl xl:text-7xl font-extrabold leading-[1.1] mb-8 tracking-tight">
        <span class="inline-block bg-gradient-to-r from-gray-900 via-blue-800 to-gray-900 bg-clip-text text-transparent animate-gradient-shift bg-[length:200%_100%]">
          Optimiza tu Negocio
        </span>
      </h1>

      <!-- Subtitle -->
      <p class="scroll-reveal text-lg sm:text-xl lg:text-2xl text-gray-600 leading-relaxed max-w-4xl mx-auto mb-12">
        Descubre las <strong class="text-blue-700">oportunidades ocultas</strong> en tu presencia digital con nuestro diagnóstico express y <strong class="text-cyan-600">paquetes de mejora</strong> enfocados en resultados.
      </p>

      <!-- Quick Stats -->
      <div class="scroll-reveal grid grid-cols-2 md:grid-cols-4 gap-4 max-w-3xl mx-auto">
        {[
          { value: '20', label: 'Puntos de análisis' },
          { value: '24h', label: 'Entrega del diagnóstico' },
          { value: '100%', label: 'Accionable' },
          { value: 'Gratis', label: 'Diagnóstico express' }
        ].map((stat, index) => (
          <div 
            class="p-4 rounded-2xl bg-white/80 backdrop-blur-xl border border-blue-100/50 shadow-lg"
            style={`animation-delay: ${index * 100}ms;`}
          >
            <div class="text-2xl font-bold bg-gradient-to-r from-blue-600 to-cyan-500 bg-clip-text text-transparent">{stat.value}</div>
            <div class="text-xs text-gray-600 mt-1">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Diagnóstico Express Section -->
  <section class="relative py-24 lg:py-32 overflow-hidden">
    <!-- Background Effects -->
    <div class="absolute inset-0 bg-gradient-to-b from-white via-blue-50/20 to-white"></div>
    <div class="absolute inset-0 opacity-[0.03]">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_30%_40%,rgba(59,130,246,0.4),transparent_60%)]"></div>
    </div>

    <div class="relative max-w-7xl mx-auto px-6">
      <!-- Section Header -->
      <div class="text-center mb-16 scroll-reveal">
        <div class="inline-flex items-center gap-3 px-6 py-3 rounded-full bg-gradient-to-r from-green-500 to-emerald-400 shadow-lg mb-6">
          <span class="text-3xl">✓</span>
          <span class="text-white font-bold text-lg tracking-wide">Diagnóstico Express</span>
        </div>
        <h2 class="text-3xl md:text-4xl lg:text-5xl font-extrabold text-gray-900 mb-4">
          Checklist de <span class="bg-gradient-to-r from-blue-600 to-cyan-500 bg-clip-text text-transparent">20 puntos</span>
        </h2>
        <p class="text-xl text-gray-700 max-w-3xl mx-auto leading-relaxed">
          Análisis <strong class="text-green-600">gratuito</strong> de tu presencia digital en 5 áreas clave. Recibe un reporte accionable en menos de 24 horas.
        </p>
      </div>

      <!-- Checklist Grid -->
      <div class="grid md:grid-cols-2 lg:grid-cols-5 gap-6 mb-12">
        {diagnosticChecklist.map((section, index) => (
          <div 
            class="scroll-reveal group relative p-6 rounded-3xl bg-white/80 backdrop-blur-xl border border-gray-100 shadow-xl hover:shadow-2xl transition-all duration-500 hover:-translate-y-2 overflow-hidden"
            style={`animation-delay: ${index * 100}ms;`}
          >
            <!-- Gradient Overlay -->
            <div class="absolute inset-0 bg-gradient-to-br from-blue-500/5 to-cyan-500/5 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
            
            <!-- Category Title -->
            <h3 class="relative text-lg font-bold text-gray-900 mb-4 pb-3 border-b border-gray-200">
              {section.category}
            </h3>

            <!-- Items List -->
            <ul class="relative space-y-2.5">
              {section.items.map(item => (
                <li class="flex items-start gap-2 text-sm text-gray-600">
                  <svg class="w-5 h-5 text-green-500 shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                  </svg>
                  <span>{item}</span>
                </li>
              ))}
            </ul>

            <!-- Decorative corner -->
            <div class="absolute -top-4 -right-4 w-20 h-20 bg-gradient-to-br from-green-400/10 to-transparent rounded-full blur-xl opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
          </div>
        ))}
      </div>

      <!-- Free Badge CTA -->
      <div class="scroll-reveal text-center">
        <div class="inline-flex items-center gap-3 px-8 py-4 rounded-2xl bg-gradient-to-r from-green-50 to-emerald-50 border-2 border-green-200 shadow-xl">
          <svg class="w-8 h-8 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <div class="text-left">
            <div class="text-sm font-semibold text-green-700 uppercase tracking-wide">100% Gratuito</div>
            <div class="text-xs text-gray-600">Sin compromiso • Entrega en 24 horas</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Paquetes de Optimización -->
  <section class="relative py-24 lg:py-32 overflow-hidden">
    <!-- Background Effects -->
    <div class="absolute inset-0 bg-gradient-to-b from-white via-cyan-50/20 to-white"></div>
    <div class="absolute inset-0 opacity-[0.03]">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_70%_50%,rgba(6,182,212,0.4),transparent_60%)]"></div>
    </div>

    <div class="relative max-w-7xl mx-auto px-6">
      <!-- Section Header -->
      <div class="text-center mb-16 scroll-reveal">
        <div class="inline-flex items-center gap-3 px-6 py-3 rounded-full bg-gradient-to-r from-blue-600 to-cyan-500 shadow-lg mb-6">
          <span class="text-3xl">📦</span>
          <span class="text-white font-bold text-lg tracking-wide">Paquetes de Mejora</span>
        </div>
        <h2 class="text-3xl md:text-4xl lg:text-5xl font-extrabold text-gray-900 mb-4">
          Soluciones enfocadas en <span class="bg-gradient-to-r from-blue-600 to-cyan-500 bg-clip-text text-transparent">resultados</span>
        </h2>
        <p class="text-xl text-gray-700 max-w-3xl mx-auto leading-relaxed">
          Elige el paquete que mejor se adapte a tus necesidades. Implementación rápida, medible y con soporte completo.
        </p>
      </div>

      <!-- Packages Grid -->
      <div class="grid lg:grid-cols-2 gap-8 max-w-6xl mx-auto">
        {packages.map((pkg, index) => (
          <div 
            class={`scroll-reveal group relative p-8 lg:p-10 rounded-3xl bg-white/90 backdrop-blur-xl border-2 shadow-2xl hover:shadow-3xl transition-all duration-500 hover:-translate-y-3 overflow-hidden ${pkg.highlight ? 'border-blue-500 lg:scale-105' : 'border-gray-200'}`}
            style={`animation-delay: ${index * 150}ms;`}
          >
            <!-- Badge if highlighted -->
            {pkg.highlight && (
              <div class="absolute -top-3 right-8">
                <div class="px-4 py-1.5 rounded-full bg-gradient-to-r from-blue-600 to-cyan-500 text-white text-xs font-bold shadow-lg">
                  {pkg.badge}
                </div>
              </div>
            )}

            <!-- Gradient Background -->
            <div class={`absolute inset-0 bg-gradient-to-br ${pkg.color} opacity-0 group-hover:opacity-5 transition-opacity duration-500`}></div>
            
            <!-- Icon Badge -->
            <div class={`relative inline-flex items-center justify-center w-20 h-20 rounded-2xl bg-gradient-to-br ${pkg.color} text-4xl shadow-xl mb-6 group-hover:scale-110 group-hover:rotate-6 transition-all duration-300`}>
              {pkg.icon}
            </div>

            <!-- Package Name -->
            <h3 class="relative text-3xl font-bold text-gray-900 mb-3">
              Paquete {pkg.name}
            </h3>

            <!-- Price -->
            <div class="relative mb-4">
              <span class={`text-2xl font-extrabold bg-gradient-to-r ${pkg.color} bg-clip-text text-transparent`}>
                {pkg.price}
              </span>
            </div>

            <!-- Description -->
            <p class="relative text-gray-600 leading-relaxed mb-8 text-lg">
              {pkg.description}
            </p>

            <!-- Features List -->
            <ul class="relative space-y-3 mb-8">
              {pkg.features.map(feature => (
                <li class="flex items-start gap-3">
                  <svg class="w-6 h-6 text-green-500 shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                  </svg>
                  <span class="text-gray-700">{feature}</span>
                </li>
              ))}
            </ul>

            <!-- CTA Button -->
            <a 
              href="#form-diagnostico" 
              class={`relative block w-full py-4 px-6 rounded-2xl bg-gradient-to-r ${pkg.color} text-white text-center font-bold text-lg shadow-xl hover:shadow-2xl transition-all duration-300 hover:scale-105 group/btn overflow-hidden`}
            >
              <!-- Shine Effect -->
              <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover/btn:translate-x-full transition-transform duration-1000"></div>
              <span class="relative">Solicitar {pkg.name}</span>
            </a>

            <!-- Decorative elements -->
            <div class={`absolute -bottom-8 -right-8 w-32 h-32 bg-gradient-to-br ${pkg.color} rounded-full blur-3xl opacity-0 group-hover:opacity-20 transition-opacity duration-500`}></div>
          </div>
        ))}
      </div>

      <!-- Bottom Info -->
      <div class="scroll-reveal mt-16 text-center">
        <div class="inline-flex flex-wrap justify-center gap-8 text-sm text-gray-600">
          <div class="flex items-center gap-2">
            <svg class="w-5 h-5 text-blue-500" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
            </svg>
            <span>Implementación en 2-3 semanas</span>
          </div>
          <div class="flex items-center gap-2">
            <svg class="w-5 h-5 text-cyan-500" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
            </svg>
            <span>Soporte post-implementación</span>
          </div>
          <div class="flex items-center gap-2">
            <svg class="w-5 h-5 text-blue-500" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
            </svg>
            <span>Documentación completa</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Form Section -->
  <section id="form-diagnostico" class="relative py-32 overflow-hidden">
    <!-- Animated Background -->
    <div class="absolute inset-0 bg-gradient-to-br from-slate-950 via-blue-950 to-slate-950"></div>
    
    <!-- Floating Blobs -->
    <div class="absolute inset-0 overflow-hidden opacity-40">
      <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-gradient-to-br from-blue-500/30 to-cyan-500/30 rounded-full blur-3xl animate-blob-float"></div>
      <div class="absolute bottom-1/3 right-1/4 w-80 h-80 bg-gradient-to-br from-cyan-400/20 to-blue-600/20 rounded-full blur-3xl animate-blob-float-delayed"></div>
    </div>

    <!-- Grid Pattern -->
    <div class="absolute inset-0 opacity-10 bg-[linear-gradient(to_right,rgba(255,255,255,0.1)_1px,transparent_1px),linear-gradient(to_bottom,rgba(255,255,255,0.1)_1px,transparent_1px)] bg-[size:60px_60px]"></div>

    <div class="relative max-w-4xl mx-auto px-6">
      <!-- Header -->
      <div class="text-center mb-12 scroll-reveal">
        <!-- Badge -->
        <div class="inline-flex items-center gap-2 px-5 py-2.5 rounded-full bg-white/10 backdrop-blur-xl border border-white/20 mb-8 shadow-2xl">
          <span class="w-2 h-2 rounded-full bg-gradient-to-r from-green-400 to-emerald-300 animate-pulse"></span>
          <span class="text-xs font-semibold tracking-wide uppercase text-white">
            Solicita tu diagnóstico
          </span>
        </div>

        <!-- Title -->
        <h2 class="text-4xl md:text-5xl lg:text-6xl font-extrabold leading-tight mb-6">
          <span class="bg-gradient-to-r from-white via-blue-100 to-cyan-100 bg-clip-text text-transparent">
            Quiero mi diagnóstico
          </span>
        </h2>

        <!-- Description -->
        <p class="text-xl text-gray-300 max-w-2xl mx-auto leading-relaxed">
          Completa el formulario y recibe tu <strong class="text-white">análisis completo gratuito</strong> en menos de 24 horas.
        </p>
      </div>

      <!-- Form Card -->
      <div class="scroll-reveal relative">
        <div class="relative p-8 lg:p-12 rounded-3xl bg-white/95 backdrop-blur-xl border border-gray-200 shadow-2xl">
          <!-- Decorative gradient -->
          <div class="absolute inset-0 bg-gradient-to-br from-blue-500/5 to-cyan-500/5 rounded-3xl"></div>
          
          <form class="relative space-y-6" action="/api/submit-diagnostic" method="POST">
            <!-- Name & Email Row -->
            <div class="grid md:grid-cols-2 gap-6">
              <div>
                <label for="name" class="block text-sm font-semibold text-gray-700 mb-2">
                  Nombre completo *
                </label>
                <input 
                  type="text" 
                  id="name" 
                  name="name" 
                  required
                  class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all outline-none"
                  placeholder="Juan Pérez"
                />
              </div>
              <div>
                <label for="email" class="block text-sm font-semibold text-gray-700 mb-2">
                  Email *
                </label>
                <input 
                  type="email" 
                  id="email" 
                  name="email" 
                  required
                  class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all outline-none"
                  placeholder="juan@empresa.com"
                />
              </div>
            </div>

            <!-- Website URL -->
            <div>
              <label for="website" class="block text-sm font-semibold text-gray-700 mb-2">
                URL de tu sitio web *
              </label>
              <input 
                type="url" 
                id="website" 
                name="website" 
                required
                class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all outline-none"
                placeholder="https://www.tusitio.com"
              />
            </div>

            <!-- Package Selection -->
            <div>
              <label for="package" class="block text-sm font-semibold text-gray-700 mb-2">
                ¿Qué te interesa?
              </label>
              <select 
                id="package" 
                name="package"
                class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all outline-none bg-white"
              >
                <option value="diagnostic">Solo diagnóstico gratuito</option>
                <option value="performance">Paquete Performance</option>
                <option value="ads-tracking">Paquete Ads + Tracking</option>
                <option value="both">Ambos paquetes</option>
                <option value="custom">Solución personalizada</option>
              </select>
            </div>

            <!-- Message -->
            <div>
              <label for="message" class="block text-sm font-semibold text-gray-700 mb-2">
                Cuéntanos sobre tu proyecto (opcional)
              </label>
              <textarea 
                id="message" 
                name="message" 
                rows="4"
                class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all outline-none resize-none"
                placeholder="Comparte cualquier detalle relevante sobre tu negocio o necesidades..."
              ></textarea>
            </div>

            <!-- Submit Button -->
            <button 
              type="submit"
              class="group w-full relative inline-flex items-center justify-center gap-3 px-8 py-5 rounded-2xl bg-gradient-to-r from-blue-500 via-cyan-500 to-blue-600 text-white text-lg font-bold shadow-2xl hover:shadow-[0_0_50px_rgba(59,130,246,0.5)] transition-all duration-300 hover:scale-[1.02] overflow-hidden"
            >
              <!-- Shine Effect -->
              <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-1000"></div>
              
              <span class="relative">📋 Solicitar mi diagnóstico gratuito</span>
              
              <!-- Arrow -->
              <svg class="relative w-6 h-6 transform group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </button>

            <!-- Privacy Note -->
            <p class="text-center text-xs text-gray-500 leading-relaxed">
              Al enviar este formulario, aceptas nuestra política de privacidad. 
              <br />Usaremos tu información únicamente para enviarte tu diagnóstico y propuestas relevantes.
            </p>
          </form>
        </div>

        <!-- Decorative corner elements -->
        <div class="absolute -top-6 -left-6 w-32 h-32 bg-gradient-to-br from-blue-500/20 to-transparent rounded-full blur-2xl"></div>
        <div class="absolute -bottom-6 -right-6 w-32 h-32 bg-gradient-to-br from-cyan-500/20 to-transparent rounded-full blur-2xl"></div>
      </div>

      <!-- Trust Indicators -->
      <div class="scroll-reveal mt-16 flex flex-wrap justify-center gap-8 text-gray-400 text-sm">
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
          <span>100% Gratis</span>
        </div>
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-blue-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
          <span>Entrega en 24hs</span>
        </div>
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-cyan-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
          <span>Sin compromiso</span>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</BaseLayout>
